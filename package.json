{
  "name": "auto-gpt-webui",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "init": "git clone https://github.com/Torantulino/Auto-GPT.git auto-gpt",
    "build": "turbo run build",
    "start": "turbo run start",
    "format": "prettier --write \"**/*.{ts,tsx,md,py,json}\""
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "@chakra-ui/css-reset": "^2.1.1"
  },
  "packageManager": "npm@9.5.0"
}
